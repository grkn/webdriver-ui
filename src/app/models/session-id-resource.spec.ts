import { SessionIdResource } from './session-id-resource';

describe('SessionIdResource', () => {
  it('should create an instance', () => {
    expect(new SessionIdResource()).toBeTruthy();
  });
});
